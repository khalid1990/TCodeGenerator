package ${PACKAGE_NAME}

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

<#--${IMPORTS}-->

public class ${CLASS_NAME}SearchController extends SearchController {

    private static final Logger log = LoggerFactory.getLogger(${CLASS_NAME}SearchController.class);

    <#--${FIELDS}

    ${METHODS}-->
}